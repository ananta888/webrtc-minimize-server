# Aktive Tracks

Ausführbare Arbeit liegt als `todo.<track>.json` in diesem Ordner. Vor Codeänderungen wird ein passender Task auf `in_progress` gesetzt; nach vollständigem Abschluss und erfolgreichem `npm run todos:validate` wandert der unveränderte Track nach `todos/archive/`.
